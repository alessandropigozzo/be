server:
  port: 9090

spring:
  h2:
    console:
      enabled: true

keycloak:
  realm: paygoz
  auth-server-url: http://localhost:8080
  resource: springbootkeycloak
  public-client: true
  bearer-only: true